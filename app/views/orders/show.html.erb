<p>
  Дата подтверждения заказа
  <%= Date.today %>
</p>
<table>
  <thead>
    <tr>
      <th> Товар </th>
      <th> Количество </th>
      <th> Цена </th>
      <th> Дата заказа </th>
    </tr>
  </thead>
  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= find_products(order.product_id).name.to_s %></td>
        <td><%= order.amount %></td>
        <td><%= order.sum %></td>
        <td><%= order.date %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<p>
  Обцая цена заказа
  <%= @sum %>
</p>
<%= link_to 'Подтвертить', orders_confirm_orders_path,
            data: { confirm: 'Внимание! Все товары на этом на этой странице будут оплачиваться одним чеком. Вы уверены?' } %>