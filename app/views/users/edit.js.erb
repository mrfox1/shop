<% if params[:avatar].present? %>
  $('.avatar').after('<%= j render 'edit_avatar_form', user: current_user %>');
<% else %>
  $('.avatar').after('<%= j render 'edit_form', user: current_user %>');
<% end %>